/**
 * Task Assignment Module Routes
 * 
 * Registers all routes for the task assignment functionality
 */

/**
 * Registers all task assignment routes with the Express app
 * @param {object} app - Express application instance
 */
function registerRoutes(app) {
  // TODO: Import and register specific route files once implemented
  // app.use('/api/mcp/tasks', require('./taskDiscoveryRoutes'));
  // app.use('/api/mcp/tasks', require('./taskAssignmentRoutes'));
  // app.use('/api/mcp/tasks', require('./taskExecutionRoutes'));
  // app.use('/api/mcp/plans', require('./planContextRoutes'));
  
  console.log('Task Assignment routes registered');
}

module.exports = {
  registerRoutes
};
