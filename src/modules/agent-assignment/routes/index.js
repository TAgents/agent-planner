/**
 * Agent Assignment Module Routes
 * 
 * Registers all routes for the agent assignment functionality
 */

/**
 * Registers all agent assignment routes with the Express app
 * @param {object} app - Express application instance
 */
function registerRoutes(app) {
  // TODO: Import and register specific route files once implemented
  // app.use('/api/agents', require('./agentRoutes'));
  // app.use('/api/plans/:planId/agents', require('./planAssignmentRoutes'));
  // app.use('/api/plans/:planId/nodes/:nodeId/agents', require('./nodeAssignmentRoutes'));
  
  console.log('Agent Assignment routes registered');
}

module.exports = {
  registerRoutes
};
