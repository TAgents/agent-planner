# Supabase Configuration
SUPABASE_URL=your_supabase_project_url
SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_KEY=your_supabase_service_key

# Database Configuration (required for migrations)
# For local Supabase: postgresql://postgres:postgres@127.0.0.1:54322/postgres
# For hosted Supabase: Get from Supabase dashboard -> Project Settings -> Database -> Connection string (Direct connection)
DATABASE_URL=postgresql://postgres:your_password@your_host:5432/postgres

# JWT Configuration (optional, Supabase handles this)
JWT_SECRET=your_jwt_secret_for_api_tokens

# Application Configuration
PORT=3000
NODE_ENV=development
FRONTEND_URL=http://localhost:3001

ANTHROPIC_API_KEY=

# Email Configuration
# Note: Supabase handles all authentication emails (verification, password reset, etc.)
# Configure email settings in your Supabase dashboard:
# Authentication -> Email Templates & SMTP Settings
# No additional email configuration needed here!

# Storage Configuration
# Note: Supabase Storage buckets need to be created manually
# 1. Go to your Supabase dashboard
# 2. Navigate to Storage
# 3. Create a bucket named "avatars"
# 4. Set the bucket to be public for read access
